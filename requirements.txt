fastapi==0.115.0
uvicorn[standard]==0.30.6
psycopg2-binary>=2.9.10
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-jose[cryptography]==3.3.0
python-dotenv==1.0.1
pydantic[email]==2.9.2

# Testing. The recommender, true-cost and budget-split logic is pure Python
# with no database, so `pytest` from the repo root runs the whole suite.
pytest==8.3.3

# zoneinfo needs this on Windows, which ships no timezone database
tzdata>=2024.1
